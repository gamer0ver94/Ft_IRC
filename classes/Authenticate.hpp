#include <iostream>

class Authenticate{
    private :

    public :
        Authenticate();
        ~Authenticate();
        bool online;
        std::string pass;
        std::string nick;
        std::string user;
};